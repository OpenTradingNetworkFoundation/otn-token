const OTNToken = artifacts.require("./token/OTNToken.sol");

module.exports = function(deployer, network, accounts) {
    // const args = process.argv.slice();
    //
    // if (args.length > 4) {
    //     deployer.deploy(OTNToken, args[3], args[4].split(","));
    //     console.info("OTNToken deployed")
    // } else {
    //     throw new Error("OTNToken deploy failed: invalid arguments");
    // }
};
